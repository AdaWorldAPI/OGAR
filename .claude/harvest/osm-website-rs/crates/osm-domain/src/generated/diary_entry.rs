// @generated by ogar-render-askama — ruff → OGAR transpile
// source class: DiaryEntry (openstreetmap-website app/models)





//! `DiaryEntry` — canonical class generated from `ogar_vocab::diaryentry()`.
//! Fields are the ClassView × FieldMask projection; methods are the OGAR
//! `ActionDef` DO-arm (behaviour is Rust, never SurrealQL DDL).
//! DO NOT EDIT BY HAND. Re-render via `ogar-render-askama`.

/// Canonical concept name as in the OGAR codebook.
pub const CANONICAL_CONCEPT: &str = "diaryentry";

#[derive(Debug, Clone, Default, PartialEq)]
pub struct DiaryEntry {
    /// belongs_to `user`.
    pub user: Option<u64>,
    /// belongs_to `language`.
    pub language: Option<u64>,
    /// has_many `comments`.
    pub comments: Vec<u64>,
    /// has_many `visible_comments`.
    pub visible_comments: Vec<u64>,
    /// has_many `subscriptions`.
    pub subscriptions: Vec<u64>,
    /// has_many `subscribers`.
    pub subscribers: Vec<u64>,
}

impl DiaryEntry {

    /// Struct-of-methods constructor over the ClassView × FieldMask field set.
    pub fn new(user: Option<u64>, language: Option<u64>, comments: Vec<u64>, visible_comments: Vec<u64>, subscriptions: Vec<u64>, subscribers: Vec<u64>) -> Self {
        Self { user, language, comments, visible_comments, subscriptions, subscribers }
    }
}