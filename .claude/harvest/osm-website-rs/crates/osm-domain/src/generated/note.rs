// @generated by ogar-render-askama — ruff → OGAR transpile
// source class: Note (openstreetmap-website app/models)





//! `Note` — canonical class generated from `ogar_vocab::note()`.
//! Fields are the ClassView × FieldMask projection; methods are the OGAR
//! `ActionDef` DO-arm (behaviour is Rust, never SurrealQL DDL).
//! DO NOT EDIT BY HAND. Re-render via `ogar-render-askama`.

/// Canonical concept name as in the OGAR codebook.
pub const CANONICAL_CONCEPT: &str = "note";

#[derive(Debug, Clone, Default, PartialEq)]
pub struct Note {
    /// belongs_to `author`.
    pub author: Option<u64>,
    /// has_many `comments`.
    pub comments: Vec<u64>,
    /// has_many `all_comments`.
    pub all_comments: Vec<u64>,
    /// has_many `subscriptions`.
    pub subscriptions: Vec<u64>,
    /// has_many `subscribers`.
    pub subscribers: Vec<u64>,
}

impl Note {

    /// Struct-of-methods constructor over the ClassView × FieldMask field set.
    pub fn new(author: Option<u64>, comments: Vec<u64>, all_comments: Vec<u64>, subscriptions: Vec<u64>, subscribers: Vec<u64>) -> Self {
        Self { author, comments, all_comments, subscriptions, subscribers }
    }
}