// @generated by ogar-render-askama — ruff → OGAR transpile
// source class: Node (openstreetmap-website app/models)





//! `Node` — canonical class generated from `ogar_vocab::osm_node()`.
//! Fields are the ClassView × FieldMask projection; methods are the OGAR
//! `ActionDef` DO-arm (behaviour is Rust, never SurrealQL DDL).
//! DO NOT EDIT BY HAND. Re-render via `ogar-render-askama`.

/// Canonical concept name as in the OGAR codebook.
pub const CANONICAL_CONCEPT: &str = "osm_node";

#[derive(Debug, Clone, Default, PartialEq)]
pub struct Node {
    /// belongs_to `changeset`.
    pub changeset: Option<u64>,
    /// has_many `old_nodes`.
    pub old_nodes: Vec<u64>,
    /// has_many `way_nodes`.
    pub way_nodes: Vec<u64>,
    /// has_many `ways`.
    pub ways: Vec<u64>,
    /// has_many `element_tags`.
    pub element_tags: Vec<u64>,
    /// has_many `old_way_nodes`.
    pub old_way_nodes: Vec<u64>,
    /// has_many `ways_via_history`.
    pub ways_via_history: Vec<u64>,
    /// has_many `containing_relation_members`.
    pub containing_relation_members: Vec<u64>,
    /// has_many `containing_relations`.
    pub containing_relations: Vec<u64>,
}

impl Node {
    /// Canonical codebook id for this class.
    pub const CLASS_ID: u16 = 0x0F01;

    /// Struct-of-methods constructor over the ClassView × FieldMask field set.
    pub fn new(changeset: Option<u64>, old_nodes: Vec<u64>, way_nodes: Vec<u64>, ways: Vec<u64>, element_tags: Vec<u64>, old_way_nodes: Vec<u64>, ways_via_history: Vec<u64>, containing_relation_members: Vec<u64>, containing_relations: Vec<u64>) -> Self {
        Self { changeset, old_nodes, way_nodes, ways, element_tags, old_way_nodes, ways_via_history, containing_relation_members, containing_relations }
    }

    /// OGAR action `index` (DO-arm).
    pub fn index(&self) {
        // TODO: port `index` from Api::NodesController
    }

    /// OGAR action `show` (DO-arm).
    pub fn show(&self) {
        // TODO: port `show` from Api::NodesController
    }

    /// OGAR action `create` (DO-arm).
    pub fn create(&self) {
        // TODO: port `create` from Api::NodesController
    }

    /// OGAR action `update` (DO-arm).
    pub fn update(&self) {
        // TODO: port `update` from Api::NodesController
    }

    /// OGAR action `destroy` (DO-arm).
    pub fn destroy(&self) {
        // TODO: port `destroy` from Api::NodesController
    }
}