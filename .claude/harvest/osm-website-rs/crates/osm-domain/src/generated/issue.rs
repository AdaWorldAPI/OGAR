// @generated by ogar-render-askama — ruff → OGAR transpile
// source class: Issue (openstreetmap-website app/models)





//! `Issue` — canonical class generated from `ogar_vocab::issue()`.
//! Fields are the ClassView × FieldMask projection; methods are the OGAR
//! `ActionDef` DO-arm (behaviour is Rust, never SurrealQL DDL).
//! DO NOT EDIT BY HAND. Re-render via `ogar-render-askama`.

/// Canonical concept name as in the OGAR codebook.
pub const CANONICAL_CONCEPT: &str = "issue";

#[derive(Debug, Clone, Default, PartialEq)]
pub struct Issue {
    /// belongs_to `reportable`.
    pub reportable: Option<u64>,
    /// belongs_to `reported_user`.
    pub reported_user: Option<u64>,
    /// belongs_to `user_resolved`.
    pub user_resolved: Option<u64>,
    /// belongs_to `user_updated`.
    pub user_updated: Option<u64>,
    /// has_many `reports`.
    pub reports: Vec<u64>,
    /// has_many `comments`.
    pub comments: Vec<u64>,
}

impl Issue {

    /// Struct-of-methods constructor over the ClassView × FieldMask field set.
    pub fn new(reportable: Option<u64>, reported_user: Option<u64>, user_resolved: Option<u64>, user_updated: Option<u64>, reports: Vec<u64>, comments: Vec<u64>) -> Self {
        Self { reportable, reported_user, user_resolved, user_updated, reports, comments }
    }
}