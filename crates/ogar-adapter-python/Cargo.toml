[package]
name = "ogar-adapter-python"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Materializes the OGAR capability surface (class ids, domain action tables, hot-plug resolution, V3 facet decoder) as a self-contained generated Python module for foreign consumers -- the plug-and-play pattern without a Rust/OGAR dependency."

[dependencies]
ogar-vocab = { path = "../ogar-vocab" }
