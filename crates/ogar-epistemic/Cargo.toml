[package]
name = "ogar-epistemic"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
authors.workspace = true
rust-version.workspace = true
description = "Compile-time classid reservation and the named 24-axis epistemic basis (v3) for the EpistemicBassin24 magnitude register. Authority-side half of the plug-and-play contract: lance-graph-contract's epistemic_bassin stays zero-dependency, this crate reserves the basis classid and names the axes; the armed parity check on the lance-graph side asserts both sides byte-for-byte."

[dependencies]

[dev-dependencies]
ogar-dismech = { path = "../ogar-dismech" }
ogar-obo = { path = "../ogar-obo" }
